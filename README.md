# HexPOS

HexPOS is a modern Point of Sale (POS) system. It features a microservices-based architecture consisting of a client application (`client-pos`), an edge API gateway (`api-edge`), and a backend transactional API (`api-transactional`).
